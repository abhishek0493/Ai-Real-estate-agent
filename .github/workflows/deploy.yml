# Deploy Pipeline
name: Deploy

on:
  push:
    branches: [main]

jobs:
  # ----------------------------------
  # Build and push to ECR
  # ----------------------------------
  build-and-push:
    runs-on: ubuntu-latest
    steps: []
    # TODO: Build Docker image, push to ECR

  # ----------------------------------
  # Deploy to ECS
  # ----------------------------------
  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps: []
    # TODO: Update ECS task definition, trigger service rollout
